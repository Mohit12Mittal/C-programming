#include<stdio.h>
int main()
{
   int num;
   scanf("%d",&num);
   int A[num];
   for(int i=0;i<num;i++)
   {
       scanf("%d",&A[i]);
   }
   for(int i=0;i<num;i++)
   {
       for(int j=i+1;j<num;j++)
       {
           if(A[i]==A[j])
           {
               printf("%d ",A[j]);
           }
       }
   }
    return 0;
}
